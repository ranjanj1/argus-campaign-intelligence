# LLM
OPENAI_API_KEY=sk-...

# PostgreSQL
POSTGRES_USER=argus
POSTGRES_PASSWORD=changeme
POSTGRES_DB=argus

# Neo4j
NEO4J_PASSWORD=changeme

# Redis
REDIS_URL=redis://localhost:6379

# JWT (dev only — use Cognito in prod)
JWT_SECRET=changeme
